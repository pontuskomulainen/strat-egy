[1mdiff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml[m
[1mindex 98bb666..5cded58 100644[m
[1m--- a/.github/workflows/ci.yml[m
[1m+++ b/.github/workflows/ci.yml[m
[36m@@ -53,12 +53,20 @@[m [mjobs:[m
       # measure. These measure whether it cheated.[m
 [m
       - name: No skipped or focused tests[m
[32m+[m[32m        if: github.event_name == 'pull_request'[m
         run: |[m
[31m-          if git diff --name-only origin/main...HEAD -- '**/*.test.*' '**/*.spec.*' \[m
[31m-             | xargs -r grep -nE '\.skip\(|\.todo\(|\.only\(|xit\(|xdescribe\(' ; then[m
[32m+[m[32m          FILES=$(git diff --name-only origin/main...HEAD -- '**/*.test.*' '**/*.spec.*')[m
[32m+[m[32m          if [ -z "$FILES" ]; then[m
[32m+[m[32m            echo "No test files changed."[m
[32m+[m[32m            exit 0[m
[32m+[m[32m          fi[m
[32m+[m[32m          HITS=$(echo "$FILES" | xargs grep -nE '\.skip\(|\.todo\(|\.only\(|xit\(|xdescribe\(' || true)[m
[32m+[m[32m          if [ -n "$HITS" ]; then[m
[32m+[m[32m            echo "$HITS"[m
             echo "::error::A test was skipped, focused, or marked todo"[m
             exit 1[m
           fi[m
[32m+[m[32m          echo "Clean."[m
 [m
       - name: Test-count ratchet[m
         # .ratchet/test-count.json is committed and the agent cannot edit it[m
